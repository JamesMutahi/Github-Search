import { ProfileDirectiveDirective } from './profile-directive.directive';

describe('ProfileDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
